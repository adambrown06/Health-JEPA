# Cohort Compass

Monorepo for the Cohort Compass platform.

| Area | Path |
|------|------|
| **Frontend** (Next.js) | [`frontend/`](frontend/) |
| **Backend** (FastAPI, JEPA, workers) | [`backend/`](backend/) |
| **Executive technical summary** | [`EXECUTIVE_TECHNICAL_SUMMARY.md`](EXECUTIVE_TECHNICAL_SUMMARY.md) |

## Quick start

- **Frontend:** `cd frontend && npm install && npm run dev`
- **Backend:** `cd backend`, create a virtualenv, `pip install -r requirements.txt`, run the API per `main.py`

---

*The Git history on GitHub includes the prior frontend-only layout; the canonical app now lives under `frontend/`.*
